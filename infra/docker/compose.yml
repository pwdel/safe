services:
  coding:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    stdin_open: true
    tty: true
    command: ["sleep", "infinity"]
    volumes:
      - /srv/workspaces/forks:/workspace
